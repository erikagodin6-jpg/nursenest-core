/**
 * Audit alias: dynamic new-grad guide loaded from API.
 * Implementation lives in `new-grad/seo-guide-page.tsx` (graceful fetch error handling).
 */
export { default } from "./new-grad/seo-guide-page";
