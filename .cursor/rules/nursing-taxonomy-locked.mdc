---
description: Locked deterministic nursing taxonomy — no AI classification for hub / write paths
globs: nursenest-core/src/lib/taxonomy/**/*.ts,nursenest-core/src/lib/pathways/pathway-learning-structure.ts,nursenest-core/src/lib/lessons/pathway-lesson-body-system-groups.ts
---

# Nursing taxonomy (non‑negotiable)

- **Single source of truth**: `nursenest-core/src/lib/taxonomy/nursing-taxonomy-classifier.ts` — rule-based only (regex / keyword dictionaries). **Do not** add embeddings, vector similarity, or LLM calls to assign `domain` / `categoryId`.
- **Hub grouping** uses `classifyPathwayLessonRecordForHub` (title + topic + sections corpus), not ad hoc string concat in UI.
- **Legacy entry point** `classifyLearningTopic` in `pathway-learning-structure.ts` delegates to `classifyNursingContent` — keep it thin; extend rules in the taxonomy module only.
- **Writes**: admin batch import (`content-pipeline/batch`) runs `validatePathwayLessonTaxonomyBeforeWrite`. Do not bypass without an explicit product decision and tests.
- **Ambiguous content** maps to `taxonomy-review-required` — never silently default to Professional Practice for “unknown” clinical-ish copy.
