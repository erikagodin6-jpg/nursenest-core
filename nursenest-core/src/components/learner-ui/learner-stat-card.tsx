/**
 * Semantic stat / KPI tile — reuses {@link AnalyticsMetricTile} (chart accents c1–c5).
 * Prefer `accent` rotation across a row so dashboards stay multi-hue per guardrails.
 */
export { AnalyticsMetricTile as LearnerStatCard } from "@/components/student/product/analytics-metric-tile";
