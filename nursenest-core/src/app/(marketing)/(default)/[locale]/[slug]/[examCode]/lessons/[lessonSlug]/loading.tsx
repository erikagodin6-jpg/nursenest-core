import ExamPathwayLessonsSegmentLoading from "../loading";

/** Same shell as hub/topic while lesson document resolves (separate segment = clearer transitions). */
export default ExamPathwayLessonsSegmentLoading;
