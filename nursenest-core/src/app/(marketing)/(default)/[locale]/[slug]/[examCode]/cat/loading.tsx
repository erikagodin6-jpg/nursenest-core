import { CatPageSkeleton } from "@/components/skeletons/hub-page-skeleton";

/**
 * Suspense / Next.js loading fallback for the CAT practice intro page.
 */
export default function ExamPathwayCatLoading() {
  return <CatPageSkeleton />;
}
