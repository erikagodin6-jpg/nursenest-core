/**
 * Route `loading.tsx` wraps the page segment; `/pricing` now resolves after `searchParams` only,
 * with heavy i18n/FAQ in a nested Suspense. A full skeleton here mostly duplicated the plan grid
 * skeleton — keep this slot empty so navigations do not flash a second pricing skeleton.
 */
export default function PricingLoading() {
  return null;
}
