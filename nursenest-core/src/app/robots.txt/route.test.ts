/**
 * Contract tests for `src/app/robots.txt/route.ts` — mirrors {@link ../lib/seo/robots-route-source.contract.test.ts}
 * so `node --import tsx --test src/app/robots.txt/route.test.ts` works from repo root.
 */
import "../../lib/seo/robots-route-source.contract.test.ts";
