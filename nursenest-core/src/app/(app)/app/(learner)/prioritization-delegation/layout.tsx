import type { ReactNode } from "react";
import "@/app/learner-prioritization-system.css";

export default function PrioritizationDelegationLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
