import type { ReactNode } from "react";
import "@/app/learner-pharmacology-system.css";

export default function PharmacologyLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
