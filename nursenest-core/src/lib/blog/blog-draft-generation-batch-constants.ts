/** Max topics per create request (admin paste batch). */
export const DRAFT_BATCH_MAX_TOPICS = 150;
/** Max items processed in one POST to stay within serverless timeouts. */
export const DRAFT_BATCH_MAX_ITEMS_PER_PROCESS = 8;
