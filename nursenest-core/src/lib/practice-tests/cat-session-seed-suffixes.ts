/**
 * Namespace suffixes for `shuffleSeeded` / pool seeds so tests can import without loading `cat-session`
 * (which pulls server-only dependencies).
 */
export const GUIDED_PRACTICE_ORDER_SEED_SUFFIX = ":guided-practice-order-v1";
