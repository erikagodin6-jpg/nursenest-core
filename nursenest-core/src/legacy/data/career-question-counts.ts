/**
 * Stub: legacy `client` used manifest-backed counts. Core marketing uses i18n labels when counts are 0.
 */
export function getQuestionCount(_slug: string): number {
  return 0;
}

export function getQuestionCountDisplay(_slug: string): string {
  return "0";
}
