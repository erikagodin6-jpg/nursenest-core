# Local environment setup

For **session signing** (`AUTH_SECRET` / `NEXTAUTH_SECRET`), public URLs, and production vs build behavior, see:

- [auth-secret-runtime-setup.md](./auth-secret-runtime-setup.md)

Run from `nursenest-core`:

```bash
npm run env:check
```

