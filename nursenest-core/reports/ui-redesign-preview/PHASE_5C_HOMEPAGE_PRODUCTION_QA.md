# Phase 5C — Homepage production QA (blockers)

**Scope:** Close homepage production QA blockers via **i18n parity** for premium homepage sections. **No homepage redesign**, **no auth / entitlements / routing / SEO generators / Prisma / pricing / leaf logo** changes.

## Files changed

| Path | Change |
|------|--------|
| `tools/i18n/marketing/locale/marketing-de.json` | Professional neutral **German** strings for **122 keys** covering `pages.home.hero.*` and `pages.home.premium.*` used by the premium homepage components (hero panel, stats line placeholders `{{count}}`, clinical depth, pathways, bottom CTA, readiness, study ecosystem, learner trust). Keys preserved; ICU placeholders match English (`{{count}}` unchanged). |
| `client/public/i18n/*.json`, `nursenest-core/public/i18n/*.json` | Regenerated by **`npm run i18n:compile`** (do not hand-edit). |

**Canonical sources (unchanged):** `tools/i18n/marketing/marketing-en.json` — overlay parity enforced by compile-time normalization.

## Keys / locales fixed (summary)

- **`pages.home.hero.*`:** Eyebrow, `headlinePremium`, `subheadingPremium`, `subheading2`, stats lines (`statsLine.questions` / `lessons` with **`{{count}}`** retained), hero panel (ECG strip labels, mini-cards, readiness/streak/mastered, **`panel.masteredValue`**).
- **`pages.home.premium.bottomCta.*`:** Assurances, body, headings, CTAs, tile labels/titles.
- **`pages.home.premium.clinicalDepth.*`:** Eyebrow through priority cue and all six cards (`pathophysiology`, `diagnostics`, `redFlags`, `interventions`, `medications`, `pearls`).
- **`pages.home.premium.pathways.*`:** Shared intro plus RN / PN / NP / international RN / allied titles, subtitles, bodies, CTAs.
- **`pages.home.premium.readiness.*`:** Section copy, dashboard/CAT CTAs, preview block, metrics, **domain labels** (localized where generic English remained).
- **`pages.home.premium.studyEcosystem.*`:** Eyebrow, heading, body, all four steps (`read` / `recall` / `practice` / `assess`) labels, titles, bodies, CTAs.
- **`pages.home.premium.trust.*`:** Body and testimonial cards (`rn` / `pn` / `np`).

**Placeholder parity:** `npm run i18n:validate:production` confirms **`{{count}}`** (and any other mustache names) match merged English for every locale key.

## Validation commands (nursenest-core)

| Command | Result |
|---------|--------|
| `npm run typecheck:critical` | **PASS** |
| `npm run test:homepage` | **PASS** (13 tests, 1 skipped) |
| `npm run build` | **Not completed in this environment** — see Build log summary |

Root **`npm run i18n:compile`** + **`npm run i18n:validate:production`**: **PASS** (22 locales, strict empty strings).

## Build log summary

1. **First `npm run build`:** Failed with `Another next build process is already running` (concurrent lock).
2. **Retry:** `next build` terminated with **exit 137** (process **SIGKILL**, typical **OOM** during Turbopack compile after lesson-index gate).
3. **Retry with `NN_SKIP_LESSON_INDEX_BUILD=1`:** Still **OOM (137)** during `next build`. Classified as **repo-wide / CI-resource**, **not** homepage-i18n slice.

Prebuild **i18n** gate (`i18n:compile` + `root-prebuild-i18n-validate`) succeeds independently.

## Playwright (`homepage-production-smoke.spec.ts`, `homepage-premium-quality.spec.ts`)

| Setting | Value |
|---------|--------|
| `BASE_URL` | `http://127.0.0.1:3000` |
| Dev server | `AUTH_SECRET` set inline for Auth.js; `NN_SKIP_DEV_AUTH_SECRET=1`; **`npm run dev:next`** |

**Outcome:** All five tests **failed** with `net::ERR_CONNECTION_REFUSED` — Playwright workers started **after** the dev server process had stopped / port closed (long-running homepage compile under memory pressure). Not attributed to i18n string edits.

**Suggested local rerun:** Stable terminal with `AUTH_SECRET` set, `npm run dev:next`, then:

```bash
cd nursenest-core
BASE_URL=http://127.0.0.1:3000 npx playwright test tests/e2e/public/homepage-production-smoke.spec.ts tests/e2e/public/homepage-premium-quality.spec.ts --project=chromium --workers=1
```

Or **`npm run build && npm run start`** when build completes on a machine with sufficient RAM.

## Screenshots → `reports/ui-redesign-preview/`

**Not generated in-session:** Dev server lifecycle was unstable under load; production build did not finish for artifact-based smoke.

**Recommended captures** (when server is stable):

1. Desktop ~1280×800 — full homepage (`/`).
2. Mobile viewport **375×800** — same URL (matches premium-quality overflow test).
3. **Dark mode:** Capture only if a **visible theme toggle exists on the marketing homepage shell** in your branch (no dedicated toggle observed in this QA pass — document skip if absent).

Copy PNGs into **`nursenest-core/reports/ui-redesign-preview/`** when that directory is writable (see `preview-screenshots/README.md`). This document lives under **`docs/ui-redesign-preview/`** because `reports/` may be ignored in some workspaces.

## Blockers

| Area | Type |
|------|------|
| **`npm run build` OOM (137)** | Repo-wide / runner capacity — **not** introduced by locale JSON edits. |
| **Playwright connection refused** | Environment / server lifecycle — **not** homepage copy placeholders. |
| **Screenshots** | Blocked on stable **`npm run dev:next`** or successful **`npm run start`** after build. |

## Compliance confirmation

- **No homepage redesign** — translation overlay updates only.
- **No auth / entitlements / routing / SEO / Prisma / pricing / leaf logo** changes.
- **Ephemeral `AUTH_SECRET`** was used only to exercise Auth.js during local dev QA attempts — **not** committed.
