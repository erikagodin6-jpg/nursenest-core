#!/usr/bin/env sh
# Husky 9+: do not source _/husky.sh (deprecated).
cd "$(git rev-parse --show-toplevel)" || exit 1
echo "Skipping pre-push checks to avoid duplicate build/typecheck pressure on the small build VM."
exit 0
