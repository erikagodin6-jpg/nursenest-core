#!/usr/bin/env sh

echo "Running pre-push verification..."
NODE_OPTIONS="--max-old-space-size=4096" npx tsc --noEmit || exit 1

# Optional full build guard for local pushes:
# HUSKY_RUN_BUILD=1 git push
if [ "${HUSKY_RUN_BUILD:-0}" = "1" ]; then
  npm run build || exit 1
fi
