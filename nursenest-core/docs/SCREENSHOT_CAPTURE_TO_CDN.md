# Screenshot capture → CDN

Canonical procedure and slot mapping live at the repository root:

**[`/docs/SCREENSHOT_CAPTURE_TO_CDN.md`](../../docs/SCREENSHOT_CAPTURE_TO_CDN.md)**

Use `npm run capture:marketing-screenshots` from this package (`nursenest-core/`) so Playwright resolves correctly.
